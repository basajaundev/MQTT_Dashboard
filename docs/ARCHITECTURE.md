# MQTT Dashboard - Architecture Diagram

## 📊 General Flowchart

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        ARRANQUE DE LA APLICACIÓN                            │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────────┐
                    │  1. monkey.patch_all()        │  ← gevent patching
                    │  2. Inicializar BD (SQLite)   │
                    │  3. Cargar configuración      │  ← Lee settings de BD
                    │  4. Iniciar scheduler (paused)│
                    └───────────────────────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────────┐
                    │  socketio.run(app, :5000)     │  ← Flask-SocketIO server
                    └───────────────────────────────┘
                                    │
            ┌───────────────────────┼───────────────────────┐
            │                       │                       │
            ▼                       ▼                       ▼
    ┌───────────────┐    ┌─────────────────┐    ┌─────────────────┐
    │ Browser       │    │ MQTT Broker     │    │ Scheduled Tasks │
    │ (WebSocket)   │    │ (Paho MQTT)     │    │ (APScheduler)   │
    └───────────────┘    └─────────────────┘    └─────────────────┘
```

---

## ⚙️ Settings System

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        SISTEMA DE CONFIGURACIÓN                             │
└─────────────────────────────────────────────────────────────────────────────┘

 Settings almacenados en tabla 'settings' (key/value):
 
 ┌─────────────────────────────────────────────────────────────────────────────┐
 │  CATEGORÍA      │  KEY                    │  DEFAULT     │  DESCRIPCIÓN    │
 ├─────────────────┼─────────────────────────┼──────────────┼─────────────────┤
 │  General        │  refresh_interval       │  30          │  Segundos       │
 │                 │  max_missed_pings       │  2           │  Ping tolerancia│
 ├─────────────────┼─────────────────────────┼──────────────┼─────────────────┤
 │  Toast          │  toast_enabled          │  true        │  Notificaciones │
 │                 │  toast_duration         │  5           │  Segundos       │
 │                 │  toast_position         │  top-right   │  Posición       │
 │                 │  toast_animation        │  fade        │  Animación      │
 │                 │  toast_types            │  all         │  Tipos mostrar  │
 ├─────────────────┼─────────────────────────┼──────────────┼─────────────────┤
 │  Backup         │  auto_backup_enabled    │  false       │  Auto backup    │
 │                 │  auto_backup_interval   │  24          │  Horas          │
 │                 │  auto_backup_keep       │  7           │  Backups a keep │
 ├─────────────────┼─────────────────────────┼──────────────┼─────────────────┤
 │  MQTT           │  mqtt_keepalive         │  60          │  Segundos       │
 │                 │  mqtt_reconnect_delay   │  5           │  Segundos       │
 │                 │  mqtt_default_qos       │  1           │  QoS 0/1/2      │
 │                 │  mqtt_clean_session     │  true        │  Clean session  │
 └─────────────────┴─────────────────────────┴──────────────┴─────────────────┘

 Lectura en Python:
     from src.globals import config
     keepalive = int(config['settings'].get('mqtt_keepalive', 60))

 Lectura en JavaScript:
     const settings = state.config?.settings || {};
     const keepalive = settings.mqtt_keepalive || 60;
```

---

## 🌐 MQTT Connection Flow

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                     CONEXIÓN MQTT CON CONFIGURACIÓN                         │
└─────────────────────────────────────────────────────────────────────────────┘

                                    │
                                    ▼
                    ┌───────────────────────────────────┐
                    │  1. Leer settings de config       │
                    │     • mqtt_keepalive = 60         │
                    │     • mqtt_reconnect_delay = 5    │
                    │     • mqtt_clean_session = true   │
                    │     • mqtt_default_qos = 1        │
                    └───────────────────────────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────────────┐
                    │  2. Crear cliente MQTT            │
                    │  mqtt.Client(clean_session=...)    │
                    └───────────────────────────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────────────┐
                    │  3. Conectar al broker            │
                    │  client.connect(broker, port,     │
                    │     keepalive=mqtt_keepalive)     │
                    └───────────────────────────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────────────┐
                    │  4. Configurar reconnect delay    │
                    │  reconnect_delay_set(             │
                    │     min=delay, max=delay*2)       │
                    └───────────────────────────────────┘
```

---

## 🌐 Client Connection Flow

```
┌──────────┐                         ┌─────────────┐                         ┌─────────────┐
│ Browser  │                         │ Flask-      │                         │ MQTT Broker │
│          │  1. connect             │ SocketIO    │                         │             │
│          │ ───────────────────────>│             │                         │             │
│          │                         │             │                         │             │
│          │  2. emit('state_update')│<────────────│<──────── 3. on_connect  │             │
│          │<───────────────────────│             │                         │             │
│          │                         │             │                         │             │
│          │  3. WebSocket events    │             │  4. Subscribe topics   │             │
│          │ ◄────────────────────── │◄────────────│<────────────────────── │             │
│          │                         │             │                         │             │
└──────────┘                         └─────────────┘                         └─────────────┘
```

---

## 📡 MQTT Message Flow

```
┌─────────────┐      ┌─────────────────┐      ┌──────────────┐      ┌─────────────────┐
│ MQTT Broker │ ───► │ on_message()    │ ───► │ Parse topic  │ ───► │ Process PONG    │
│             │      │ callback        │      │ & payload    │      │                 │
└─────────────┘      └─────────────────┘      └──────────────┘      └─────────────────┘
                                                                      │
                                                                      ▼
                                                            ┌─────────────────┐
                                                            │ get_or_create_  │
                                                            │ device()        │
                                                            └─────────────────┘
                                                                      │
                                                                      ▼
                                                            ┌─────────────────┐
                                                            │ Update devices  │
                                                            │ dict (memory)   │
                                                            └─────────────────┘
                                                                      │
                                                                      ▼
                                                            ┌─────────────────┐
                                                            │ Publicar con    │
                                                            │ QoS=config      │
                                                            └─────────────────┘
```

---

## 📝 Device Search Flow

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        BÚSQUEDA DE DISPOSITIVOS                             │
└─────────────────────────────────────────────────────────────────────────────┘

                                    │
                                    ▼
                    ┌───────────────────────────────────┐
                    │  Input: deviceSearch.value        │
                    │  Event: input                     │
                    └───────────────────────────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────────────┐
                    │  renderDevices(searchTerm)        │
                    │                                   │
                    │  Filtrar por:                     │
                    │  • device.name                   │
                    │  • device.id                     │
                    │  • device.location               │
                    │                                   │
                    └───────────────────────────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────────────┐
                    │  Mostrar "Sin resultados" si      │
                    │  no hay coincidencias             │
                    └───────────────────────────────────┘
```

---

## 🔐 Whitelist and Groups Flow

```
┌──────────┐      ┌─────────────────┐      ┌─────────────────┐      ┌─────────────┐
│ Browser  │ ───► │ handle_add_to_  │ ───► │ add_to_         │ ───► │ Save to DB  │
│          │      │ whitelist()     │      │ whitelist()     │      │ (SQLite)    │
│          │      └─────────────────┘      └─────────────────┘      └─────────────┘
│          │                                                               │
│          │<───────────────────────────────────────────────────────────────│
│          │                  broadcast_full_update()                      │
└──────────┘                                                               │
                                                                           │
                      ┌──────────────────────────────────────────────────────┘
                      │
                      ▼
             ┌─────────────────┐      ┌─────────────────┐      ┌─────────────┐
             │ get_whitelist() │ ───► │ JOIN con Groups │ ───► │ Return with │
             │ (lectura)       │      │ para nombres    │      │ group_name  │
             └─────────────────┘      └─────────────────┘      └─────────────┘
                                                                           │
                                                                           ▼
                                                                 ┌─────────────────┐
                                                                 │ Frontend:       │
                                                                 │ renderDevices() │
                                                                 │ (agrupa por     │
                                                                 │  group_name)    │
                                                                 └─────────────────┘
```

---

## 📋 Dashboard Devices Flow

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        renderDevices() Flow                                 │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────────────┐
                    │  state.accessLists.whitelist      │
                    │  = [{id, location, group_name}]   │
                    └───────────────────────────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────────────┐
                    │  1. Crear Set de keys permitidas  │
                    │     allowedKeys = {               │
                    │       "esp32@room1",              │
                    │       "esp8266@kitchen"           │
                    │     }                             │
                    └───────────────────────────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────────────┐
                    │  2. Mapear key → group_name       │
                    │     deviceToGroup = {             │
                    │       "esp32@room1": "Cocina",    │
                    │       "esp8266@kit": "Cocina",    │
                    │       "sensor@garage": null       │
                    │     }                             │
                    └───────────────────────────────────┘
                                    │
            ────────────────────────┼───────────────────────
            │                       │                       │
            ▼                       ▼                       ▼
    ┌───────────────┐     ┌─────────────────┐     ┌─────────────────┐
    │ Filtrar por   │     │ Agrupar por     │     │ Renderizar con  │
    │ searchTerm    │     │ grupo           │     │ headers         │
    │ (opcional)    │     │                 │     │                 │
    └───────────────┘     └─────────────────┘     └─────────────────┘
```

---

## ⏰ Auto-Refresh (PING) Flow

```
┌─────────────────┐      ┌─────────────────┐      ┌─────────────────┐
│ Scheduler       │ ───► │ handle_ping_    │ ───► │ Set todos los   │
│ (cada 30s)      │      │ all_devices()   │      │ dispositivos a  │
│                 │      │                 │      │ 'offline'       │
└─────────────────┘      └─────────────────┘      └─────────────────┘
                                                          │
                                                          ▼
                                                ┌─────────────────┐
                                                │ Publicar PING   │
                                                │ QoS=config      │
                                                └─────────────────┘
                                                          │
                              ┌───────────────────────────┼───────────────────────────┐
                              ▼                           ▼                           ▼
                    ┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
                    │ Device responde │     │ Device responde │     │ Device NO       │
                    │ PONG (online)   │     │ STATUS          │     │ responde        │
                    └─────────────────┘     └─────────────────┘     └─────────────────┘
                              │                           │                           │
                              ▼                           ▼                           ▼
                    ┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
                    │ Update status   │     │ Process sensor  │     │ Contador        │
                    │ = 'online'      │     │ data & save     │     │ missed_pings++   │
                    └─────────────────┘     └─────────────────┘     └─────────────────┘
                                                                              │
                                                                              ▼
                                                                    ┌─────────────────┐
                                                                    │ Si >= max:      │
                                                                    │ status = offline│
                                                                    │ + event BD      │
                                                                    └─────────────────┘
```

---

## 💾 Backup System Flow

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        SISTEMA DE BACKUP                                     │
└─────────────────────────────────────────────────────────────────────────────┘

    ┌─────────────────────────────────────────────────────────────────────────┐
    │  BACKUP MANUAL                    │  BACKUP AUTOMÁTICO                  │
    ├───────────────────────────────────┼─────────────────────────────────────┤
    │  User hace click en              │  Scheduler ejecuta                  │
    │  "Backup Ahora"                  │  scheduled_backup_job()             │
    │                                   │                                     │
    │  ▼                               │  ▼                                  │
    │  emit('trigger_backup')          │  Cada N horas                       │
    │                                   │  (configurable)                     │
    │  ▼                               │                                     │
    │  BackupManager.create_backup()   │  ▼                                  │
    │                                   │  BackupManager.create_backup()      │
    │                                   │                                     │
    │  Comprime: dashboard.db.gz        │  Comprime: dashboard.db.gz          │
    │  Guarda en: /backups/             │  Guarda en: /backups/               │
    │  rotate_backups(N)                │  rotate_backups(N)                  │
    └───────────────────────────────────┴─────────────────────────────────────┘

 Formato: dashboard_backup_YYYYMMDD_HHMMSS.db.gz
```

---

## 🔄 Socket.IO Events Summary

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        EVENTOS SOCKET.IO                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  CLIENTE → SERVIDOR (camelCase)          SERVIDOR → CLIENTE (snake_case)   │
│  ───────────────────────────────         ────────────────────────────────   │
│                                                                             │
│  • connect                                • state_update                    │
│  • request_initial_state                  • devices_update                  │
│  • add_to_whitelist                       • history_update                  │
│  • remove_from_whitelist                  • alerts_update                   │
│  • add_group                              • topics_update                   │
│  • update_group                           • task_update                     │
│  • delete_group                           • mqtt_status                     │
│  • mqtt_connect                           • mqtt_config_updated             │
│  • mqtt_disconnect                        • new_alert                       │
│  • mqtt_subscribe                         • device_history_response         │
│  • mqtt_unsubscribe                       • device_events_response          │
│  • mqtt_publish                           • known_devices_update            │
│  • mqtt_ping_all                          • groups_update                   │
│  • update_mqtt_config                     • access_lists_update             │
│  • save_settings                          • backup_config_updated           │
│  • update_backup_config                   • backups_list                    │
│  • trigger_backup                         • backup_complete                  │
│  • change_password                        • password_changed                │
│  • add_alert                              • error                           │
│  • update_alert                           • ...                             │
│  • delete_alert                                                           │
│  • task_create                                                           │
│  • task_edit                                                             │
│  • task_delete                                                           │
│  • task_toggle                                                           │
│  • request_device_history                                                │
│  • get_device_events                                                      │
│  • reboot_device                                                          │
│  • request_single_device_status                                           │
│  • update_device_alias                                                    │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 🗄️ Data Model (Simplified)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           MODELO DE DATOS                                   │
└─────────────────────────────────────────────────────────────────────────────┘

┌──────────┐      1:N      ┌──────────┐
│  Server  │◄──────────────│  Device  │
│          │               │          │
└──────────┘               └──────────┘
    │                           │
    │                           │ N:1
    │                           ▼
    │                   ┌──────────────┐
    │                   │   Whitelist  │
    │                   │──────────────│
    │                   │ device_id    │
    │                   │ location     │
    │                   │ group_id (FK)│◄─────┐
    │                   └──────────────┘      │
    │                                         │ N:1
    │                           ┌─────────────┘
    │                           ▼
    │                   ┌──────────┐
    │                   │  Group   │
    │                   │          │
    │                   └──────────┘
    │
    ├── N:1 ───► Task
    ├── N:1 ───► Subscription
    ├── N:1 ───► Alert
    ├── N:1 ───► MessageTrigger
    ├── N:1 ───► SensorData (histórico)
    └── N:1 ───► DeviceEvent

┌──────────┐       ┌──────────┐       ┌──────────┐       ┌──────────┐
│  Setting │       │DeviceEvent│      │  Admin   │       │DeviceAlias│
└──────────┘       └──────────┘       └──────────┘       └──────────┘
   (key/value)
```

---

## 📝 Flow Summary

| Component | Description |
|------------|-------------|
| **Startup** | Initialize DB, load config from settings, start server |
| **Settings** | All config stored in settings table (key/value) |
| **MQTT Config** | keepalive, reconnect_delay, clean_session, default_qos |
| **Client** | Browser connects via WebSocket, receives state_update |
| **MQTT** | Paho MQTT receives messages, calls callbacks |
| **Devices** | Only shown if in whitelist (registered devices) |
| **Groups** | JOIN with Whitelist to show group name |
| **Search** | Real-time filtering by name, id, location |
| **WebSocket** | Bidirectional client↔server events |
| **Scheduler** | Auto-refresh every 30s with PING |
| **Backup** | Manual or automatic with compression |

---

## 🔧 Key Files

| File | Purpose |
|------|---------|
| `app.py` | Application entry point |
| `src/socket_handlers.py` | WebSocket event handlers |
| `src/mqtt_callbacks.py` | MQTT message callbacks |
| `src/persistence.py` | Database CRUD operations |
| `src/models.py` | SQLAlchemy models |
| `src/database.py` | DB initialization and migrations |
| `backup_db.py` | Backup manager with compression |
| `static/js/main.js` | Frontend entry point |
| `static/js/modules/core/socket.js` | WebSocket client & state |
| `static/js/modules/core/dom.js` | DOM element cache |
| `static/js/modules/core/state.js` | Global state object |
| `static/js/modules/ui/ui.js` | UI rendering |
| `static/js/modules/events/events.js` | Event handlers |
| `static/js/modules/device/dashboard.js` | Device grid rendering |
| `static/js/modules/modals/modals.js` | Modal management |
